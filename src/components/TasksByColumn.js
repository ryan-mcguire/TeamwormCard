// TasksByColumn.js
import React, { Component } from 'react';
import SortTable from './SortTable.js'

export default class TasksByColumn extends Component {

    render() {
        console.log("  In TaskLists.render(), tasks has " + this.props.tasks.length);
        return (
        );
    }
}
